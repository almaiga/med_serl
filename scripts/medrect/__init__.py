# MEDRECT SFT scripts — Stage 0: Medical error detection fine-tuning
# Uses MEDRECT dataset to ground the model in error localization
# before MedPRM chain training (Stage 1) and self-play RL (Stage 2).
